/**
 * Enum describing the form field types
 */
export enum FieldTypeEnum {

    BOOLEAN,
    CATEGORY,
    CONFIRM_PASSWORD,
    EMAIL,
    FILE,
    NUMBER,
    PASSWORD,
    PHONE,
    SELECT,
    STRING

}
